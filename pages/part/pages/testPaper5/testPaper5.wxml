<!--pages/part//pages/testPaper5/testPaper5.wxml-->
<view class="container">
	<image class="bg" mode="aspectFill" src="{{bgImg}}"></image>
	<view class="center">
		<image class="centerImg" src="{{bgImg1}}"></image>
		<view class="name">{{name}}</view>
		<!-- <view class="nameList">{{nameArr[0]}} {{nameArr[1]}} {{nameArr[2]}}</view> -->

		<view class="footer">
			<view class="btnList" wx:if='{{!start}}' bindtap="pause">暂停</view>
			<!-- <view class="btnList" bindtap="start">开启</view> -->
		<button class="btn"  wx:else open-type='getUserInfo' bindgetuserinfo="getUserInfo">确认信息</button>

		</view>
	</view>
</view>